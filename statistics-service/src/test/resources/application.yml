hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 20000

spring:
  data:
    mongodb:
      database: pcs-db
      port: 0

rates:
  url: https://api.exchangeratesapi.io