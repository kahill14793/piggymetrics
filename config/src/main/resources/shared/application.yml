logging:
  level:
    org:
      springframework:
        security: INFO

hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 10000

eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://172.168.72.3:8761/eureka/

spring:
  main:
    allow-circular-references: true
    

feign:
  hystrix:
    enabled: true
    
camunda:
  bpm:
    admin-user:
      password: noodles
      id: noodles
    metrics:
      db-reporter-activate: 'false'
    database:
      schema-update: 'true'
      
minio:
  secretKey: 123456789
  endpoint: http://172.168.72.5:9990
  accessKey: 123456789
  domain: https://afiex.online
  
